# Server-only: RSC, Route Handlers, Server Actions (`src/lib/api.ts`)
API_BASE_URL=

# Client / browser: must use `NEXT_PUBLIC_` prefix so Next.js inlines it at build time (`src/lib/api.ts` in Client Components).
NEXT_PUBLIC_API_BASE_URL=

spring:
  application:
    name: Bazaar API

  datasource:
    url: jdbc:mysql://localhost:3306/bazaar_db?useSSL=false&serverTimezone=UTC
    username: bazaar_user
    password: password123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update       # Automatically create/update tables based on entities (dev only)
    show-sql: true